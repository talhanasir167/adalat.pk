<div class="home-div">
  <div class="d-flex justify-content-between flex-wrap">
    <div class="d-flex mb-2">
      <div class="align-self-center"><%= image_tag(current_user.user_avatar, class: 'rounded-circle size-15vw')%></div>
      <div class="ps-2 align-self-center text-center">
        <span class="fw-semibold display-6 ms-3"><%= current_user.full_name %></span><br/>
        <%= link_to "Update Account", edit_user_registration_path(current_user), class: "btn btn-link text-primary"%>
      </div>
    </div>
  </div>
</div>
