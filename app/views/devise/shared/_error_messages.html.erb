<% if resource.errors.any? %>
  <div id="error_explanation" data-turbo-cache="false">
    <ul class="text-danger">
      <% resource.errors.full_messages.each do |message| %>
        <li><%= message.gsub('User summary', '').strip.capitalize %></li>
      <% end %>
    </ul>
  </div>
<% end %>
